name: "🐛 Bug report"
description: "报告一个可复现的问题（代码 / 文档 / Demo 都算）"
title: "[bug] "
labels: ["type:bug", "status:backlog"]
body:
  - type: markdown
    attributes:
      value: |
        感谢你提交 Issue！请尽量提供完整信息，方便我们快速复现与修复。
  - type: dropdown
    id: priority
    attributes:
      label: 优先级 (Priority)
      description: 请评估该问题的紧急程度
      options:
        - "prio:P0 (严重阻塞/安全/交付风险)"
        - "prio:P1 (本迭代必须完成)"
        - "prio:P2 (重要但可延期)"
        - "prio:P3 (低优先级/优化项)"
    validations:
      required: true
  - type: dropdown
    id: module
    attributes:
      label: 模块归属 (Module)
      description: 问题发生在哪个模块？
      options:
        - "module:VCU"
        - "module:MCU"
        - "module:BMS"
        - "module:OBC"
        - "module:DCDC"
        - "module:EMS"
        - "module:CAN"
        - "module:Diag"
        - "module:Bootloader"
        - "module:Calibration"
        - "Other"
    validations:
      required: true
  - type: dropdown
    id: offline
    attributes:
      label: 线下环境 (Offline) - 可选
      description: 如果涉及线下设备或环境，请选择
      options:
        - "None"
        - "offline:vehicle (实车)"
        - "offline:bench (台架)"
        - "offline:hil (HIL)"
        - "offline:sil (SIL)"
        - "offline:lab (实验室)"
        - "offline:flash (刷写)"
    validations:
      required: false
  - type: input
    id: env
    attributes:
      label: 环境信息
      description: 例如 Windows 11 / macOS 14 / Ubuntu 22.04 + Python 3.11 + Git 2.4x
      placeholder: "OS / Python / Git / 其他关键工具版本"
    validations:
      required: true
  - type: textarea
    id: what
    attributes:
      label: 发生了什么
      description: 描述你遇到的现象（尽量清晰）
    validations:
      required: true
  - type: textarea
    id: reproduce
    attributes:
      label: 如何复现
      description: 请贴出最小复现步骤（命令/路径/输入）
      placeholder: |
        1) git clone ...
        2) python demos/...
        3) ...
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: 你期望看到什么
    validations:
      required: false
  - type: textarea
    id: logs
    attributes:
      label: 日志 / 报错信息
      description: 粘贴完整报错（不要只截取一部分）
      render: shell
    validations:
      required: false

name: "🚀 Feature / Requirement"
description: "提出新功能需求或任务目标"
title: "[feat] "
labels: ["type:requirement", "status:backlog"]
body:
  - type: markdown
    attributes:
      value: |
        请清晰描述需求背景和目标，帮助我们更好地理解价值。
  - type: dropdown
    id: priority
    attributes:
      label: 优先级 (Priority)
      options:
        - "prio:P0 (严重阻塞/安全/交付风险)"
        - "prio:P1 (本迭代必须完成)"
        - "prio:P2 (重要但可延期)"
        - "prio:P3 (低优先级/优化项)"
    validations:
      required: true
  - type: dropdown
    id: module
    attributes:
      label: 模块归属 (Module)
      options:
        - "module:VCU"
        - "module:MCU"
        - "module:BMS"
        - "module:OBC"
        - "module:DCDC"
        - "module:EMS"
        - "module:CAN"
        - "module:Diag"
        - "module:Bootloader"
        - "module:Calibration"
        - "Other"
    validations:
      required: true
  - type: textarea
    id: background
    attributes:
      label: 背景与现状
      description: 为什么需要这个功能？现在有什么痛点？
    validations:
      required: true
  - type: textarea
    id: goal
    attributes:
      label: 目标与预期
      description: 希望达成什么效果？有什么具体的验收标准？
      placeholder: |
        - 实现 VCU 上电自检逻辑
        - 验收：上电后 CAN 报文发出 0x123，无故障码
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: 方案建议 (可选)
      description: 如果有技术方案或实现思路，请简要描述
    validations:
      required: false
  - type: textarea
    id: context
    attributes:
      label: 补充上下文
      description: 相关的 Issue、文档链接、参考资料等
    validations:
      required: false
